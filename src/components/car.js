const Car = ({company , country}) => {
    return (
        <div>
            <div>Your car company is {company}</div>
            <div>Your car country is {country}</div>
        </div>
    );
}

export default Car;